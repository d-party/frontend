// Allow side-effect imports of global stylesheets (e.g. `import "./globals.css"`).
// Next.js handles CSS at the bundler level; this declaration satisfies the
// TypeScript compiler's check for side-effect imports of non-code modules.
declare module "*.css";
